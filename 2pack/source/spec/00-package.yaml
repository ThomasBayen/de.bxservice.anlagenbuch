# Paket-Header: wird zu <idempiere ...> in PackOut.xml
#
# License: AGPL-3.0-or-later. Volltext in der LICENSE-Datei im Repo-Root;
# SPDX-Header steht in jedem Skript. iDempiere-2Pack hat selbst kein
# License-Feld in <idempiere ...>, deshalb hier nur als Kommentar; die
# menschenlesbare Variante steht in 2pack/source/PackageDoc.xml.
package:
  name: de.bxservice.anlagenbuch
  version: 1.0.0
  idempiere_version: 11.0.0
  description: Anlagenbuch — Wartungs- und Fehlerberichts-System
  author: Jakob Bayen KG / BX Service
  author_email: tbayen@bayen.de
  entity_type: U          # User-Records, beim Kunden änderbar
  table_prefix: BXS_
  default_access_level: 3 # Client+Org (alle Anlagenbuch-Tabellen)
